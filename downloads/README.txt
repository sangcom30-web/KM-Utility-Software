เอาไฟล์ .zip ของโปรแกรมจริงมาวางในโฟลเดอร์นี้ ให้ชื่อไฟล์ตรงกับที่ index.html อ้างอิงไว้:

- CounterExport.zip
- DocScanUtility.zip
- FTPScanUtility.zip

ถ้าจะใช้ชื่อไฟล์อื่น (เช่น CounterExport_22_08_02.zip) ให้แก้ href ใน index.html ให้ตรงกันด้วย
